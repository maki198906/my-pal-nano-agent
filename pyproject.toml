[project]
name = "nanopal"
version = "0.1.0"
description = "Minimal code-first agent powered by uv + OpenRouter"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "rich>=13.0.0",
]

[tool.uv]
package = false